import {connect} from "react-redux";

import AdjustmentView from "./Adjustment.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(AdjustmentView);
