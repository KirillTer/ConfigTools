import {connect} from "react-redux";

import BonusOptOutView from "./BonusOptOut.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(BonusOptOutView);
