import {connect} from "react-redux";

import BonusesView from "./Bonuses.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(BonusesView);
