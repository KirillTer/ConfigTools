import {connect} from "react-redux";

import ChatStatusView from "./ChatStatus.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(ChatStatusView);
