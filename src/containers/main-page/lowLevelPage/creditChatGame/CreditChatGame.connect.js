import {connect} from "react-redux";

import CreditChatGameView from "./CreditChatGame.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(CreditChatGameView);
