import {connect} from "react-redux";

import ForgetStatusView from "./ForgetStatus.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(ForgetStatusView);
