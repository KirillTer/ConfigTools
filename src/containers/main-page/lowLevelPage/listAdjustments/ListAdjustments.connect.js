import {connect} from "react-redux";

import ListAdjustmentsView from "./ListAdjustments.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(ListAdjustmentsView);
