import {connect} from "react-redux";

import LoyaltyLevelsView from "./LoyaltyLevels.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(LoyaltyLevelsView);
