import {connect} from "react-redux";

import PromotionHistoryView from "./PromotionHistory.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(PromotionHistoryView);
