import {connect} from "react-redux";

import StuckGamesView from "./StuckGames.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(StuckGamesView);
