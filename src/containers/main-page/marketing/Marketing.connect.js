import {connect} from "react-redux";

import MarketingView from "./Marketing.view";


const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(MarketingView);
